#!/bin/bash

export CLIC_ROOT="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"

usage() {
        echo "Usage:"
	echo "-u --user <name>         set up user account <name> on cloud computers for use with clic"
	echo "-n --namescheme <name>   use <name> as the base name for cloud instances"
	echo "-c --cloud               installing to a cloud computer"
	echo "-s                       scripted, don't ask for confirmation"
	echo "-h --help                print this help message"
}

export user=`whoami`
export namescheme=`hostname`
export cloud=false
export script=false
ARGS=`getopt -n clic-install -o hscn:u: --long help,cloud,namescheme:,user: -- "$@"`
if [ $? != 0 ] ; then
        usage
        exit 1
fi
eval set -- "$ARGS"
while true; do
        case $1 in
                -h | --help)
                        usage
                        exit 0
                        ;;
		-s)
			export script=true;
			shift
			;;
                -c | --cloud)
                        export cloud=true
                        shift
                        ;;
                -n | --namescheme)
                        export namescheme="$2"
                        shift 2
                        ;;
                -u | --user)
                        export user=$2
                        shift 2
                        ;;
                --)
                        shift
                        break
                        ;;
                *)
                        break
                        ;;
        esac
done

if [ "$script" == false ]; then
	echo -n "Installing clic using "
	if [ "$cloud" == false ]; then
		echo -n "the google cloud instance named $namescheme as the basis for all compute nodes, and the user account $user on $namescheme for ssh communication. The instance $namescheme must be currently running and "
	else
		echo -n "this cloud instance ($namescheme) as the basis for both the head and compute nodes, and the user account $user on this instance for ssh communication. This instance must "
	fi
	echo "already have any desired computational software installed to it."
	echo "Press ENTER to continue, or CTRL-c to exit."
	read ans
fi

if [ -n "`uname -a | grep -i Debian`" ]; then
   OS="debian"
elif [ -n "`uname -a | grep -i ubuntu`" ]; then
   OS="ubuntu"
elif [ -e "/etc/redhat-release" ]; then
   OS="centos"
else
   echo "Unidentified OS! Exiting."
   exit
fi
echo "Installing for $OS"

command -v gcloud >/dev/null 2>&1 || $CLIC_ROOT/scripts/$OS/setup_gcloud
$CLIC_ROOT/scripts/common/setup_gcloud

$CLIC_ROOT/scripts/$OS/install_slurm
$CLIC_ROOT/scripts/common/install_clic
if [ "$cloud" == false ]; then
	$CLIC_ROOT/scripts/common/create_cloud_snapshot
	echo "Please ensure that tcp port 6817 is allowed through the firewall to this machine."
fi
